<div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-10">
  <div class="sm:flex sm:items-center">
    <div class="sm:flex-auto">
      <h1 class="text-2xl font-semibold leading-6 text-gray-900">接客履歴</h1>
      <p class="mt-2 text-sm text-gray-700">録音、文字起こし、メモを含む全ての営業履歴一覧です。</p>
    </div>
    <div class="mt-4 sm:ml-16 sm:mt-0 sm:flex-none">
      <%= button_to "新規録音開始", interactions_path, method: :post, class: "block rounded-md bg-indigo-600 px-3 py-2 text-center text-sm font-semibold text-white shadow-sm hover:bg-indigo-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600" %>
    </div>
  </div>

  <div class="mt-8 flow-root">
    <div class="-mx-4 -my-2 overflow-x-auto sm:-mx-6 lg:-mx-8">
      <div class="inline-block min-w-full py-2 align-middle sm:px-6 lg:px-8">
        <div class="overflow-hidden shadow-lg shadow-slate-200/50 ring-1 ring-slate-900/5 sm:rounded-2xl bg-white">
          <table class="min-w-full divide-y divide-slate-200">
            <thead class="bg-slate-50">
              <tr>
                <th scope="col" class="py-3.5 pl-4 pr-3 text-left text-sm font-semibold text-slate-900 sm:pl-6">状態</th>
                <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-slate-900">開始日時</th>
                <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-slate-900">顧客</th>
                <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-slate-900">担当</th>
                <th scope="col" class="relative py-3.5 pl-3 pr-4 sm:pr-6">
                  <span class="sr-only">編集</span>
                </th>
              </tr>
            </thead>
            <tbody class="divide-y divide-slate-100 bg-white">
              <% @interactions.each do |interaction| %>
                <tr class="hover:bg-slate-50/50 transition-colors">
                  <td class="whitespace-nowrap py-4 pl-4 pr-3 text-sm font-medium text-slate-900 sm:pl-6">
                    <span class="inline-flex items-center rounded-md px-2 py-1 text-xs font-medium ring-1 ring-inset <%= interaction.recording? ? 'bg-red-50 text-red-700 ring-red-600/10' : 'bg-green-50 text-green-700 ring-green-600/20' %>">
                      <%= t("activerecord.enums.interaction.status.#{interaction.status}") %>
                    </span>
                  </td>
                  <td class="whitespace-nowrap px-3 py-4 text-sm text-slate-500"><%= l interaction.started_at, format: :short if interaction.started_at %></td>
                  <td class="whitespace-nowrap px-3 py-4 text-sm text-slate-500"><%= interaction.customer&.name || "未割当" %></td>
                  <td class="whitespace-nowrap px-3 py-4 text-sm text-slate-500"><%= interaction.user.name %></td>
                  <td class="relative whitespace-nowrap py-4 pl-3 pr-4 text-right text-sm font-medium sm:pr-6">
                    <%= link_to "詳細", interaction_path(interaction), class: "text-indigo-600 hover:text-indigo-900" %>
                  </td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</div>
