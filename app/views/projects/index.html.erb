<% content_for :title, "案件管理" %>

<div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-10">
  <div class="sm:flex sm:items-center">
    <div class="sm:flex-auto">
      <h1 class="text-2xl font-bold leading-6 text-slate-900">案件管理</h1>
      <p class="mt-2 text-sm text-slate-500">現在進行中および完了したすべての案件リストです。</p>
    </div>
    <div class="mt-4 sm:ml-16 sm:mt-0 sm:flex-none">
      <%= link_to new_project_path, class: "block rounded-lg bg-indigo-600 px-4 py-2.5 text-center text-sm font-semibold text-white shadow-md hover:bg-indigo-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600 transition-all" do %>
        <span class="mr-1">+</span> 新規案件登録
      <% end %>
    </div>
  </div>

  <div class="mt-8 flow-root">
    <div class="-mx-4 -my-2 overflow-x-auto sm:-mx-6 lg:-mx-8">
      <div class="inline-block min-w-full py-2 align-middle sm:px-6 lg:px-8">
        <div class="overflow-hidden shadow-lg shadow-slate-200/50 ring-1 ring-slate-900/5 sm:rounded-2xl bg-white">
          <table class="min-w-full divide-y divide-slate-200">
            <thead class="bg-slate-50">
              <tr>
                <th scope="col" class="py-3.5 pl-4 pr-3 text-left text-sm font-semibold text-slate-900 sm:pl-6">案件名</th>
                <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-slate-900">ステータス</th>
                <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-slate-900">顧客</th>
                <th scope="col" class="px-3 py-3.5 text-right text-sm font-semibold text-slate-900">見積金額</th>
                <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-slate-900">担当者</th>
                <th scope="col" class="relative py-3.5 pl-3 pr-4 sm:pr-6">
                  <span class="sr-only">編集</span>
                </th>
              </tr>
            </thead>
            <tbody class="divide-y divide-slate-100 bg-white">
              <% if @projects.any? %>
                <% @projects.each do |project| %>
                  <tr class="hover:bg-slate-50/50 transition-colors">
                    <td class="whitespace-nowrap py-4 pl-4 pr-3 text-sm font-medium text-slate-900 sm:pl-6">
                        <%= project.name %>
                    </td>
                    <td class="whitespace-nowrap px-3 py-4 text-sm text-slate-500">
                        <% status_color = case project.status
                           when 'pending' then 'bg-gray-100 text-gray-600 ring-gray-500/10'
                           when 'negotiating' then 'bg-blue-50 text-blue-700 ring-blue-700/10'
                           when 'contracted' then 'bg-indigo-50 text-indigo-700 ring-indigo-700/10'
                           when 'started' then 'bg-amber-50 text-amber-700 ring-amber-600/20'
                           when 'delivered' then 'bg-green-50 text-green-700 ring-green-600/20'
                           else 'bg-gray-50 text-gray-600 ring-gray-500/10'
                           end %>
                        <span class="inline-flex items-center rounded-md px-2 py-1 text-xs font-medium ring-1 ring-inset <%= status_color %>">
                            <%= t("activerecord.enums.project.status.#{project.status}") %>
                        </span>
                    </td>
                    <td class="whitespace-nowrap px-3 py-4 text-sm text-slate-500"><%= project.customer.name %></td>
                    <td class="whitespace-nowrap px-3 py-4 text-sm text-slate-900 font-mono text-right tracking-tight">
                        <%= number_to_currency(project.estimated_amount, unit: "¥", precision: 0, strip_insignificant_zeros: true) %>
                    </td>
                    <td class="whitespace-nowrap px-3 py-4 text-sm text-slate-500"><%= project.user.name %></td>
                    <td class="relative whitespace-nowrap py-4 pl-3 pr-4 text-right text-sm font-medium sm:pr-6">
                      <%= link_to "詳細", project, class: "text-indigo-600 hover:text-indigo-900 mr-3" %>
                      <%= link_to "編集", edit_project_path(project), class: "text-slate-400 hover:text-slate-600" %>
                    </td>
                  </tr>
                <% end %>
              <% else %>
                 <tr>
                    <td colspan="6" class="py-10 text-center text-slate-500">
                        <p class="text-base font-medium text-slate-900">案件がありません</p>
                        <p class="mt-1 text-sm text-slate-500">新しい案件を作成して管理を始めましょう。</p>
                        <div class="mt-6">
                            <%= link_to new_project_path, class: "inline-flex items-center rounded-md bg-indigo-600 px-3 py-2 text-sm font-semibold text-white shadow-sm hover:bg-indigo-500" do %>
                                <span class="mr-2">+</span> 新規案件登録
                            <% end %>
                        </div>
                    </td>
                 </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</div>
