<div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-10">
  <div class="sm:flex sm:items-center">
    <div class="sm:flex-auto">
      <h1 class="text-2xl font-semibold leading-6 text-gray-900">分析レポート</h1>
      <p class="mt-2 text-sm text-gray-700">広告媒体ごとのパフォーマンス分析レポートです。</p>
    </div>
  </div>

  <div class="mt-8 flow-root">
    <div class="-mx-4 -my-2 overflow-x-auto sm:-mx-6 lg:-mx-8">
      <div class="inline-block min-w-full py-2 align-middle sm:px-6 lg:px-8">
        <div class="overflow-hidden shadow ring-1 ring-black/5 sm:rounded-lg">
          <table class="min-w-full divide-y divide-gray-300">
            <thead class="bg-gray-50">
              <tr>
                <th scope="col" class="py-3.5 pl-4 pr-3 text-left text-sm font-semibold text-gray-900 sm:pl-6">広告媒体</th>
                <th scope="col" class="px-3 py-3.5 text-right text-sm font-semibold text-gray-900">獲得顧客数</th>
                <th scope="col" class="px-3 py-3.5 text-right text-sm font-semibold text-gray-900">成約数</th>
                <th scope="col" class="px-3 py-3.5 text-right text-sm font-semibold text-gray-900">成約率</th>
              </tr>
            </thead>
            <tbody class="divide-y divide-gray-200 bg-white">
              <% @media_reports.each do |report| %>
                <tr>
                  <td class="whitespace-nowrap py-4 pl-4 pr-3 text-sm font-medium text-gray-900 sm:pl-6"><%= report[:name] %></td>
                  <td class="whitespace-nowrap px-3 py-4 text-sm text-gray-500 text-right"><%= report[:leads] %></td>
                  <td class="whitespace-nowrap px-3 py-4 text-sm text-gray-500 text-right"><%= report[:contracts] %></td>
                  <td class="whitespace-nowrap px-3 py-4 text-sm font-bold text-gray-900 text-right"><%= report[:contract_rate] %>%</td>
                </tr>
              <% end %>
              <% if @media_reports.empty? %>
                 <tr>
                    <td colspan="4" class="py-4 text-center text-gray-500">データがありません。</td>
                 </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</div>
